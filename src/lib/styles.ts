/**
 * Shared style helpers.
 * Colors come from the site palette (see PALETTE.md / index.css).
 */
export const DOTTED_BG: React.CSSProperties = {
    backgroundImage:
        'radial-gradient(circle at 1px 1px, rgba(18,20,24,0.08) 1px, transparent 0)',
    backgroundSize: '24px 24px',
    backgroundColor: 'var(--paper)',
};
